# Orbital configuration — /opt/orbital/etc/orbital.env
#
# Copy this file to /opt/orbital/etc/orbital.env and adjust paths.
# The orbital-guard user must have read access to referenced paths.

# Android SDK location (required)
# Set ACL: sudo chmod -R +a "orbital-guard allow read,execute,list,search" $ANDROID_HOME
ANDROID_HOME=/Users/YOU/Library/Android/sdk

# Java home (optional — uses system default if not set)
# /Library/Java/... is typically world-readable, no ACL needed.
# JAVA_HOME=/Library/Java/JavaVirtualMachines/zulu-17.jdk/Contents/Home

# Gradle user home (defaults to /opt/orbital/cache/gradle)
GRADLE_USER_HOME=/opt/orbital/cache/gradle

# TLS — HTTPS with self-signed certificate
# Certs owned by orbital-guard at /opt/orbital/etc/certs/
ORBITAL_TLS_CERT=/opt/orbital/etc/certs/orbital-server.crt
ORBITAL_TLS_KEY=/opt/orbital/etc/certs/orbital-server.key

# Audit log (append-only security log)
# ORBITAL_AUDIT_LOG=/opt/orbital/log/audit.log

# Signing profiles — env vars follow the pattern SIGNING_<UPPER>_<FIELD>.
# Set ACL: sudo chmod +a "orbital-guard allow read" /path/to/keystore.jks
# SIGNING_AOSP_KEYSTORE=/path/to/aosp.keystore.jks
# SIGNING_AOSP_KEYSTORE_PASSWORD=changeme
# SIGNING_AOSP_KEY_ALIAS=platform
# SIGNING_AOSP_KEY_PASSWORD=changeme
